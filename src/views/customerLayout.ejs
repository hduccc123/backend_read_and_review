<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <title>
        <%= title || "Book Store" %>
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
        }

        header {
            background: linear-gradient(to right, #00c6ff, #0072ff);
            padding: 10px 20px;
            color: white;
        }

        .logo {
            font-weight: bold;
            font-size: 24px;
            color: gold;
            text-decoration: none;
        }

        .nav-menu a {
            color: white;
            margin-right: 20px;
            text-decoration: none;
            font-weight: 500;
        }

        .nav-menu a.active {
            color: red;
        }

        .search-bar {
            max-width: 600px;
            flex: 1;
        }

        .icon {
            color: white;
            margin-left: 20px;
            cursor: pointer;
        }

        .cart-badge {
            background: red;
            color: white;
            border-radius: 50%;
            padding: 2px 6px;
            font-size: 12px;
            position: relative;
            top: -10px;
            left: -5px;
        }
    </style>
</head>

<body>
    <!-- HEADER -->
    <header class="d-flex align-items-center">
        <!-- Logo -->
        <a href="/" class="logo me-3">BOOK365.VN</a>

        <!-- Search -->
        <form class="d-flex search-bar">
            <select class="form-select me-2" style="max-width: 150px;">
                <option selected>Danh mục</option>
                <option value="1">Sách in</option>
                <option value="2">Sách điện tử</option>
            </select>
            <input type="text" class="form-control me-2" placeholder="Tìm kiếm sách...">
            <button class="btn btn-light"><i class="bi bi-search"></i></button>
        </form>

        <!-- User -->
        <div class="d-flex align-items-center">
            <a href="/login" class="icon"><i class="bi bi-person-circle"></i> Đăng ký/Đăng nhập</a>
            <div class="icon position-relative">
                <i class="bi bi-cart"></i>
                <span class="cart-badge">0</span>
            </div>
        </div>
    </header>

    <!-- NAV MENU -->
    <nav class="nav-menu d-flex justify-content-center p-2" style="background:#00BFFF;">
        <a href="#" class="active">Sàn sách in</a>
        <a href="#">Sàn sách điện tử</a>
        <a href="#">Gian hàng sách</a>
        <a href="#">Nhà tài trợ</a>
        <a href="#">Tin tức - Sự kiện</a>
        <a href="#">Sách đã xem</a>
        <a href="#">Tra cứu đơn hàng</a>
    </nav>

    <!-- CONTENT -->
    <main class="container mt-4">
        <%- body %>
    </main>

    <!-- FOOTER -->
    <footer class="text-center p-3 mt-4 bg-light">
        <p>© 2025 Nhà Sách Online</p>
    </footer>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>